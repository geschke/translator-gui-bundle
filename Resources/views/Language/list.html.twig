{% extends "GeschkeAdminTranslatorGUIBundle::base_admin.html.twig" %}
{% form_theme form 'GeschkeAdminTranslatorGUIBundle:Form:fields.html.twig' %}


{%  block admincontent %}
    {% trans %}Messages{% endtrans %}


    {% for flashMessage in app.session.flashbag.get('message') %}

            <div class="alert alert-info">
            {{ flashMessage }}
        </div>
    {% endfor %}

    <table class="table table-striped ">
        <thead>
        <tr>
            <th>Key</th>
            <th>Translation</th>
               <th>Aktion</th>
        </tr>
        </thead>
        <tbody>

        {% for key, message in messages %}

            <tr class="message-item" data-id="{{ message.getId }}">
                <td id="message_{{ key }}">{{ message.getSourceString }}</td>
                <td id="translation_{{ key }}">{{ message.getLocaleString }}</td>
                <td>
                    <button data-locale="{{ locale }}" type="button" class="btn-action-edit btn btn-default btn-xs">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> {% trans %}Edit{% endtrans %}
                    </button>


        </td>
        </tr>
    {% endfor %}
        </tbody>
    </table>




    <!-- Modal -->
    <div id="modalTranslate" class="modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel"
         aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                    <h3 class="modal-title" id="myModalLabel"> {% trans %}Translate message{% endtrans %}</h3>
                </div>
                <div class="modal-body">

                    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
                </div>
                <div class="modal-footer">
                    <button class="btn" data-dismiss="modal" aria-hidden="true">{% trans %}Cancel{% endtrans %}</button>
                    <button class="btn btn-primary do-edit-action">{% trans %}Save Message{% endtrans %}</button>
                </div>
            </div>
        </div>
    </div>



    <script type="text/javascript">
        var bundle = "{{ bundle }}" || undefined; // maybe later: encapsulate in object or something...
        var locale = "{{ locale }}" || undefined;

        //console.log(bundle);
        //console.log(locale);


        var adminUrl = '{{ path('geschke_admin_translator_gui_homepage') }}';
        $(function () {
/*            $('.btn-action-language-add').click(function () {
                var options = {};
                bundle = $(this).parents('.bundle-item').attr('data-name');
                //console.log(bundle);
                window.location = adminUrl  + 'language-add/?bundle=' + bundle;
//                $('#modalDelete').modal(options);

            });

            $('.btn-action-delete').click(function () {
                var options = {};
                bundle = $(this).parents('.bundle-item').attr('data-name');
                locale = $(this).attr('data-locale');
                console.log(bundle);
                console.log(locale);
                $('#modalDelete').modal(options);

            });

            $('.do-delete-action').click(function () {
                $('#modalDelete').modal('hide');
                var deleteBundle = bundle;
                var deleteLocale = locale;
                bundle = undefined;
                locale = undefined;
                window.location = adminUrl  + 'language-delete/?bundle=' + deleteBundle + '&locale=' + deleteLocale;
            });

            $('.btn-action-rescan2').click(function () {
                var options = {};
                bundle = $(this).parents('.bundle-item').attr('data-name');
                locale = $(this).attr('data-locale');
                window.location = adminUrl  + 'language-rescan/?bundle=' + bundle + '&locale=' + locale;

            });
*/
            $('.btn-action-edit').click(function () {
                var modal = $('#modalTranslate');
                //        bar = modal.find('.progress-bar');
                var messageId = $(this).parents('.message-item').attr('data-id');
                console.log(messageId);
                locale = $(this).attr('data-locale');
                var url =  adminUrl  + 'language-translate/';
                $('#form_locale').val(locale);
                $('#form_message').val('');
                $('#form_translation').val('');

                modal.modal('show');

               $.ajax({
                    type: "GET",
                    url: url,
                    data: { bundle: bundle, locale: locale, messageId: messageId }
                })
                        .done(function( msg ) {


                            /*$('.rescan-ready').show();
                            $('.rescan-loading').hide();
                            if (msg.success ==  true) {
                                $('.rescan-success').show();
                                $('.rescan-error').hide();
                                console.log("ok");
                            } else {
                                $('.rescan-success').hide();
                                $('.rescan-error').show();

                                console.log("false");
                            }
                            */
                           $('#form_message').val(msg.messageTranslation.message);

                           $('#form_translation').val(msg.messageTranslation.translation);
                        });

                /*
                bar.addClass('animate');

                setTimeout(function() {
                    bar.removeClass('animate');
                    modal.modal('hide');
                }, 1500);*/
            });

            $('.do-edit-action').click(function () {
                var modal = $('#modalTranslate');
                var url =  adminUrl  + 'language-translate/';


                var data = {
                    bundle: bundle,
                    locale:  $('#form_locale').val(),
                    message: $('#form_message').val(),
                    translation:  $('#form_translation').val()
                };
console.log(data);
                $.ajax({
                    type: "POST",
                    url: url,
                    data: data
                })
                        .done(function( msg ) {
                            console.log(msg);

                            /*$('.rescan-ready').show();
                             $('.rescan-loading').hide();
                             if (msg.success ==  true) {
                             $('.rescan-success').show();
                             $('.rescan-error').hide();
                             console.log("ok");
                             } else {
                             $('.rescan-success').hide();
                             $('.rescan-error').show();

                             console.log("false");
                             }
                             */
                        });

                console.log("do-edit-action");
            });
            modal.modal('hide');


        });

    </script>


{%  endblock %}
